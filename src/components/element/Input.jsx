// import React from "react";
// import styled from "styled-components";

// const Input = ({ value, onChange, width, height, color, name, type }) => {
//   const styles = { value, onChange, width, height, color, name, type };
//   return <StInput {...styles}></StInput>;
// };

// const StInput = styled.input`
//   width: ${({ width }) => width};
//   height: ${({ height }) => height};
//   color: ${({ color }) => color};
// `;
